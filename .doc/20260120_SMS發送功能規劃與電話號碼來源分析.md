# SMS 發送功能規劃與電話號碼來源分析

**建立日期**: 2026-01-20
**問題**: 系統中有 SMS 發送基礎設施，但缺少電話號碼來源
**狀態**: 規劃階段

---

## 📋 問題摘要

### 現況
1. ✅ 資料庫有 `opportunities.contactPhone` 欄位
2. ✅ 資料庫有 `sms_logs` 表格（追蹤 SMS 發送記錄）
3. ✅ 使用 EVERY8D API 作為 SMS 服務商
4. ❌ **Slack Bot 上傳音檔時「沒有」收集電話號碼**
5. ❌ **在匯入 Salesforce 資料之前，contactPhone 大多是空值**

### 用戶回饋
> "雖然說有 opportunities.contactPhone 不過他的來源是什麼？我印象中目前這個資料應該沒有值，在我們匯入 Salesforce 資料之前"

---

## 🔍 電話號碼來源分析

### 目前「有」的來源 ✅

#### 1. **Squarespace 表單整合** (已實作)
**檔案**: `packages/services/src/lead-source/squarespace/mapper.ts:86-87`

```typescript
const contactPhone =
  getFieldValue(data, mapping, "contactPhone") || data.phone;
```

**說明**:
- 來自 Squarespace 網站的表單提交
- 欄位名稱: `phone` 或自訂 mapping 的 `contactPhone`

**覆蓋率**: 低（只有透過 Squarespace 表單的 leads）

---

#### 2. **網頁端手動建立商機** (已實作)
**檔案**: `apps/web/src/routes/opportunities/new.tsx:188-194`

```typescript
<Label htmlFor="contactPhone">電話</Label>
<Input
  id="contactPhone"
  name="contactPhone"
  type="tel"
  value={formData.contactPhone}
/>
```

**說明**:
- 業務人員在網頁上手動建立商機時可以填寫電話
- 欄位是「選填」，不強制

**覆蓋率**: 中（取決於業務人員是否填寫）

---

#### 3. **V2 資料遷移** (已實作)
**檔案**: `scripts/migration/mappers/v2-mapper.ts:70`

```typescript
contactPhone: v2Case.customerPhone || null,
```

**說明**:
- 從舊版系統遷移資料時帶過來
- 只適用於已遷移的歷史資料

**覆蓋率**: 僅限歷史資料

---

### 目前「沒有」的來源 ❌

#### 1. **Slack Bot 上傳音檔時** ❌ (問題核心)
**檔案**: `apps/slack-bot/src/utils/form-builder.ts:123-185`

**目前表單欄位**:
```typescript
const commonBlocks = [
  buildTextInput("customer_number", "客戶編號", "例如：C001、A0123", true),
  buildTextInput("customer_name", "客戶名稱", "例如：王小明咖啡店", true),
];

const productBlocks = [
  buildSelectInput("store_type", ...),      // 店鋪類型
  buildSelectInput("service_type", ...),    // 營運型態 (iCHEF only)
  buildSelectInput("staff_count", ...),     // 員工數量 (Beauty only)
  buildSelectInput("current_system", ...),  // 現有系統
];

const additionalBlocks = [
  buildSelectInput("decision_maker_present", ...), // 決策者在場
];
```

**缺少**: `contactPhone` 欄位

**影響**:
- 這是**最主要的資料來源**（80% 以上的 conversations 來自 Slack Bot 上傳）
- 沒有這個欄位 = 絕大多數情況下無法發送 SMS

---

#### 2. **Salesforce 自動同步** ❌ (未來規劃)
**說明**:
- 目前沒有從 Salesforce API 自動同步客戶電話
- 需要額外開發 Salesforce 整合

**優點**:
- 資料來源權威
- 完全自動化

**缺點**:
- 開發時間較長
- 需要 Salesforce API 權限

---

## 💡 解決方案規劃

### 方案 A: 在 Slack Bot 表單新增電話欄位 (推薦) ⭐

**時程**: 1 天
**優先級**: P0（立即執行）

#### 實作步驟

##### Step 1: 修改表單建構器
**檔案**: `apps/slack-bot/src/utils/form-builder.ts`

**變更位置**: Line 123-126

**修改後**:
```typescript
const commonBlocks = [
  buildTextInput("customer_number", "客戶編號", "例如：C001、A0123", true),
  buildTextInput("customer_name", "客戶名稱", "例如：王小明咖啡店", true),
  buildTextInput("contact_phone", "客戶電話（選填）", "例如：0912-345-678", false), // 新增
];
```

**說明**:
- 新增 `contact_phone` 欄位
- 設定為「選填」(`false`)，不強制業務填寫
- 提供範例格式引導輸入

---

##### Step 2: 修改表單解析器
**檔案**: `apps/slack-bot/src/utils/form-builder.ts`

**變更位置**: Line 244-260

**新增**:
```typescript
export interface AudioUploadMetadata {
  customerNumber: string;
  customerName: string;
  contactPhone?: string; // 新增
  productLine?: ProductLine;
  storeType?: string;
  serviceType?: string;
  staffCount?: string;
  currentSystem?: string;
  decisionMakerPresent?: string;
}

export function parseAudioUploadFormValues(
  values: Record<...>,
  productLine: ProductLine
): Partial<AudioUploadMetadata> {
  const metadata: Partial<AudioUploadMetadata> = {
    productLine,
    customerNumber: values.customer_number?.customer_number?.value,
    customerName: values.customer_name?.customer_name?.value,
    contactPhone: values.contact_phone?.contact_phone?.value, // 新增
    storeType: values.store_type?.store_type?.selected_option?.value,
    // ... 其他欄位
  };

  return metadata;
}
```

---

##### Step 3: 修改事件處理器
**檔案**: `apps/slack-bot/src/events/file.ts`

**變更位置**: 確保 `contactPhone` 被傳遞到 API

**檢查點**:
```typescript
// 確認在建立 opportunity 時包含 contactPhone
const opportunity = await apiClient.createOpportunity({
  customerNumber: metadata.customerNumber,
  companyName: metadata.customerName,
  contactPhone: metadata.contactPhone, // 確保這行存在
  // ... 其他欄位
});
```

---

##### Step 4: 測試驗收

- [ ] Slack Bot 上傳音檔時，表單顯示「客戶電話（選填）」欄位
- [ ] 填寫電話後提交，資料正確儲存到 `opportunities.contactPhone`
- [ ] 不填寫電話時，也可以正常提交（選填欄位）
- [ ] 電話格式驗證（可選）

---

### 方案 B: 網頁端補充電話功能 (補充方案)

**時程**: 0.5 天
**優先級**: P1（方案 A 完成後）

#### 實作內容

在網頁端「商機詳情」頁面新增「編輯」按鈕，允許業務人員補充電話號碼。

**檔案**: `apps/web/src/routes/opportunities/$id.tsx`

**新增功能**:
```typescript
// 編輯商機按鈕
<Button onClick={() => setIsEditing(true)}>
  編輯商機資訊
</Button>

// 編輯表單 (包含電話欄位)
{isEditing && (
  <Dialog>
    <Input
      label="客戶電話"
      value={contactPhone}
      onChange={(e) => setContactPhone(e.target.value)}
    />
    <Button onClick={handleSave}>儲存</Button>
  </Dialog>
)}
```

**優點**:
- 補充遺漏的電話號碼
- 方便業務人員後續更新

---

### 方案 C: Salesforce 自動同步 (長期規劃)

**時程**: 2-3 週
**優先級**: P2（未來需求）

#### 前置條件
- [ ] Salesforce API 存取權限
- [ ] Salesforce 中已有客戶電話資料
- [ ] 確認 Salesforce 與 iCHEF 的 customer mapping

#### 實作概念
```typescript
// 在建立 opportunity 時，從 Salesforce 取得電話
async function createOpportunityFromSalesforce(customerNumber: string) {
  const sfData = await salesforceAPI.getContact(customerNumber);

  return {
    customerNumber,
    companyName: sfData.company,
    contactPhone: sfData.phone, // 從 Salesforce 取得
    contactEmail: sfData.email,
  };
}
```

---

## 📊 覆蓋率預估

### 實作前（現況）
| 來源 | 覆蓋率 | 說明 |
|------|--------|------|
| Squarespace 表單 | 5-10% | 僅限網站表單提交 |
| 網頁端手動建立 | 10-20% | 取決於業務填寫習慣 |
| V2 遷移資料 | 歷史資料 | 只適用於已遷移資料 |
| **總計** | **~15-30%** | **大部分沒有電話** |

### 實作後（方案 A）
| 來源 | 覆蓋率 | 說明 |
|------|--------|------|
| **Slack Bot 上傳** | **70-80%** | **新增欄位** |
| Squarespace 表單 | 5-10% | 維持現狀 |
| 網頁端手動建立 | 10-20% | 維持現狀 |
| **總計** | **~85-95%** | **大幅提升** |

### 實作後（方案 A + B + C）
| 來源 | 覆蓋率 | 說明 |
|------|--------|------|
| Slack Bot 上傳 | 70-80% | 方案 A |
| 網頁端補充 | +5-10% | 方案 B |
| Salesforce 同步 | +5-10% | 方案 C |
| **總計** | **~95-100%** | **接近完整覆蓋** |

---

## 🚀 建議實作順序

### 第 1 週: 方案 A（核心功能）
1. ✅ 修改 Slack Bot 表單，新增「客戶電話」欄位
2. ✅ 測試表單提交與資料儲存
3. ✅ 部署到生產環境

**預期效果**: 覆蓋率從 ~30% → ~85%

---

### 第 2 週: SMS 發送整合
1. ✅ 實作 EVERY8D SMS 發送服務
2. ✅ Queue Worker 處理完成後發送 SMS（如果有電話）
3. ✅ 記錄發送狀態到 `sms_logs`

**預期效果**: 可以開始發送 SMS 通知

---

### 第 3 週: 方案 B（補充功能）
1. ✅ 網頁端新增「編輯商機」功能
2. ✅ 允許補充電話號碼

**預期效果**: 覆蓋率從 ~85% → ~90%

---

### 未來（方案 C，視需求而定）
1. ⏳ 評估 Salesforce 整合的必要性
2. ⏳ 如果需要，實作 Salesforce API 同步

---

## 💰 SMS 發送成本估算

### EVERY8D 計費方式
- **國內簡訊**: 每則約 1-1.5 點（約 NT$1-1.5）
- **國際簡訊**: 每則約 3-10 點

### 預估用量
假設：
- 每日處理 20 個 conversations
- 其中 85% 有電話號碼 = 17 則 SMS/day
- 每月 = 17 × 30 = 510 則 SMS/month

**月成本**: 510 × NT$1.5 = **NT$765/月**

---

## 📋 SMS 內容規劃

### 簡訊內容範例

```
【iCHEF】您好，您的銷售對話分析已完成！

案件編號: 202601-IC019
MEDDIC 分數: 50/100

查看完整報告:
https://sales-ai-web.pages.dev/conversations/abc123

如有疑問請聯繫您的業務專員。
```

**字數**: 約 70 字（1 則簡訊）

---

## 🔒 隱私與合規

### 電話號碼處理原則

1. **資料收集**:
   - 明確告知用途（發送 SMS 通知）
   - 設定為「選填」，不強制

2. **資料儲存**:
   - 加密儲存於資料庫
   - 僅限授權人員存取

3. **資料使用**:
   - 僅用於業務通知
   - 不做行銷用途
   - 不分享給第三方

4. **用戶權利**:
   - 可隨時要求刪除
   - 可選擇退出 SMS 通知

---

## 📝 待決策項目

### 1. 電話號碼格式驗證
**問題**: 是否需要驗證電話號碼格式？

**選項**:
- [ ] **不驗證** - 業務自行確保正確性（簡單，推薦）
- [ ] **基本驗證** - 檢查是否為數字，長度 10 碼
- [ ] **嚴格驗證** - 正規表達式 `09\d{8}` 或 `02-\d{7,8}`

**建議**: 基本驗證即可

---

### 2. SMS 發送時機
**問題**: 何時發送 SMS？

**選項**:
- [ ] **立即發送** - Queue Worker 完成處理後立即發送（推薦）
- [ ] **批次發送** - 每日固定時間批次發送
- [ ] **業務手動觸發** - 在 Slack 通知中提供「發送 SMS」按鈕

**建議**: 立即發送（與 Slack 通知同步）

---

### 3. SMS 內容策略
**問題**: SMS 內容應該包含什麼？

**選項**:
- [ ] **精簡版** - 只有分數 + 連結（推薦，70 字內）
- [ ] **詳細版** - 包含下一步行動（可能超過 1 則）

**建議**: 精簡版，詳細資訊透過連結查看

---

## ✅ 驗收標準

### 方案 A 驗收
- [ ] Slack Bot 表單包含「客戶電話（選填）」欄位
- [ ] 填寫電話後，資料正確儲存到 `opportunities.contactPhone`
- [ ] 不填寫電話時，仍可正常提交
- [ ] 電話號碼顯示在商機詳情頁面

### SMS 發送驗收
- [ ] Queue Worker 完成處理後，如果有電話，成功發送 SMS
- [ ] SMS 內容包含: 案件編號、分數、連結
- [ ] 發送記錄正確寫入 `sms_logs`
- [ ] 發送失敗時，有錯誤日誌

### 整體驗收
- [ ] 覆蓋率達到 85% 以上
- [ ] SMS 發送成功率 > 95%
- [ ] 用戶反饋正面

---

## 📚 相關文件

- [Slack 通知優化計畫](rosy-weaving-piglet.md)
- [EVERY8D API 文件](https://www.every8d.com/api)
- [Squarespace Webhook 整合](.doc/workflows/20260113_Phase5_Agent5_MQL_Ops開發.md)

---

**文件版本**: 1.0
**最後更新**: 2026-01-20
**維護者**: Claude & Stephen
