# Web 應用部署完成說明

**部署日期**: 2026-01-20
**版本**: v1.0

---

## 🎉 部署成功

Web 應用已成功部署到 Cloudflare Pages！

---

## 🌐 正式環境網址

### Web 應用（Cloudflare Pages）
- **生產環境**: https://sales-ai-web.pages.dev
- **登入頁面**: https://sales-ai-web.pages.dev/login
- **Dashboard**: https://sales-ai-web.pages.dev/dashboard

### API 伺服器（Cloudflare Workers）
- **API 端點**: https://sales-ai-server.salesaiautomationv3.workers.dev
- **Better Auth**: https://sales-ai-server.salesaiautomationv3.workers.dev/api/auth

---

## 🔑 登入方式

### 選項 1：使用 Google 登入（推薦）

1. 訪問：https://sales-ai-web.pages.dev/login
2. 點擊「使用 Google 登入」按鈕
3. 選擇您的 Google 帳號（請使用 @ichef.com.tw 的企業帳號）
4. 授權後自動跳轉到 Dashboard

**注意**：Google OAuth 需要在 Google Cloud Console 設定正確的回調 URL：
```
https://sales-ai-server.salesaiautomationv3.workers.dev/api/auth/callback/google
```

### 選項 2：使用 Email/Password 註冊

1. 訪問：https://sales-ai-web.pages.dev/login
2. 切換到「Sign Up」（或點擊「Need an account? Sign Up」）
3. 填寫註冊資訊：
   - **Name**: 您的姓名
   - **Email**: stephen.kao@ichef.com.tw（或其他 @ichef.com.tw 帳號）
   - **Password**: 設定您的密碼（至少 8 個字元）
4. 點擊「Sign Up」完成註冊
5. 自動登入並跳轉到 Dashboard

---

## 🔐 權限設定

系統已設定三級權限控制：

### 管理者（Admin）
- **Email**: stephen.kao@ichef.com.tw
- **權限**: 可查看所有業務的資料

### 主管（Manager）
- **Email**:
  - wade.lin@ichef.com.tw
  - benjamin.we@ichef.com.tw
- **權限**: 可查看所有業務的資料

### 一般業務（Sales）
- **權限**: 只能查看自己創建的資料

---

## 📝 查看案件 202601-IC019

登入後，您可以訪問以下網址查看完整的 MEDDIC 分析：

```
https://sales-ai-web.pages.dev/conversations/cf75684f-4f5b-4667-8e09-0cd50262d9bc
```

**頁面內容包含**：
- 📊 MEDDIC 總分與資格狀態
- 🎯 6 個維度的詳細評分（Metrics, Economic Buyer, Decision Criteria, Decision Process, Identify Pain, Champion）
- 🔍 關鍵發現列表
- 💡 客戶痛點與解決方案
- ✅ 下一步行動（iCHEF 與客戶待辦事項）
- ⚠️ 風險評估與緩解措施
- 🎓 教練建議
- 🎵 音檔播放器
- 📝 完整轉錄記錄

---

## 🚀 部署資訊

### Cloudflare Pages 專案

**專案名稱**: `sales-ai-web`

**部署 ID**: ab061d01

**生產網址**: https://sales-ai-web.pages.dev

**構建設定**:
- Build Command: `bun run build`
- Build Output Directory: `dist`
- Framework: Vite

### 環境變數

**VITE_SERVER_URL**: `https://sales-ai-server.salesaiautomationv3.workers.dev`

（已在構建時注入）

### CORS 設定

Server 的 `CORS_ORIGIN` 已更新為：
```
https://sales-ai-web.pages.dev
```

這確保了 Web 應用可以正常調用 API。

---

## 📦 部署命令

### 構建 Web 應用

```bash
cd apps/web
bun run build
```

### 部署到 Cloudflare Pages

```bash
cd apps/web
bunx wrangler pages deploy dist --project-name=sales-ai-web
```

### 更新 Server CORS 設定

1. 編輯 `apps/server/wrangler.toml`
2. 更新 `CORS_ORIGIN` 為 Pages 域名
3. 重新部署 Server：
   ```bash
   cd apps/server
   bunx wrangler deploy
   ```

---

## 🧪 測試步驟

### 1. 測試登入功能

- [ ] 訪問 https://sales-ai-web.pages.dev/login
- [ ] 看到登入頁面，包含 Google 登入按鈕和 Email 登入表單
- [ ] Google 登入按鈕可點擊
- [ ] Email 登入表單可填寫

### 2. 測試註冊功能

- [ ] 切換到 Sign Up 頁面
- [ ] 填寫註冊資訊
- [ ] 成功註冊後跳轉到 Dashboard

### 3. 測試 Dashboard

- [ ] 登入後可看到 Dashboard
- [ ] Dashboard 顯示統計資訊

### 4. 測試案件查看

- [ ] 訪問案件 202601-IC019 的連結
- [ ] 可以看到完整的 MEDDIC 分析
- [ ] 音檔可以播放
- [ ] 轉錄記錄可以查看

### 5. 測試權限控制

- [ ] Admin 帳號可以查看所有案件
- [ ] Manager 帳號可以查看所有案件
- [ ] 一般業務只能查看自己的案件

---

## 🔧 故障排除

### Q1: 登入後 API 請求失敗

**可能原因**：CORS 設定不正確

**解決方案**：
1. 檢查 `apps/server/wrangler.toml` 中的 `CORS_ORIGIN` 是否為 `https://sales-ai-web.pages.dev`
2. 重新部署 Server：
   ```bash
   cd apps/server
   bunx wrangler deploy
   ```

### Q2: Google 登入失敗

**可能原因**：Google Cloud Console 的回調 URL 未設定

**解決方案**：
1. 前往 Google Cloud Console
2. 在 OAuth 2.0 Client ID 設定中新增授權重新導向 URI：
   ```
   https://sales-ai-server.salesaiautomationv3.workers.dev/api/auth/callback/google
   ```
3. 儲存變更

### Q3: 頁面顯示空白或 404

**可能原因**：路由設定問題

**解決方案**：
1. 確認 URL 是否正確
2. 檢查瀏覽器 Console 是否有錯誤訊息
3. 重新構建並部署 Web 應用

### Q4: 無法查看案件資料

**可能原因**：未登入或權限不足

**解決方案**：
1. 確認已登入
2. 檢查使用的帳號是否在權限白名單中
3. 如果是一般業務帳號，確認該案件是由您創建的

---

## 📞 後續維護

### 更新 Web 應用

1. 修改程式碼
2. 構建：`cd apps/web && bun run build`
3. 部署：`bunx wrangler pages deploy dist --project-name=sales-ai-web`

### 更新權限白名單

1. 編輯 `apps/server/wrangler.toml` 中的 `ADMIN_EMAILS` 和 `MANAGER_EMAILS`
2. 重新部署 Server

### 檢查部署狀態

```bash
# 列出 Pages 專案
bunx wrangler pages project list

# 查看 Pages 部署記錄
bunx wrangler pages deployment list --project-name=sales-ai-web

# 查看 Server 部署資訊
cd apps/server
bunx wrangler deployments list
```

---

## 🎯 下一步建議

### 優先級 1：測試完整流程

1. 使用 stephen.kao@ichef.com.tw 註冊/登入
2. 查看案件 202601-IC019
3. 測試音檔播放
4. 確認所有功能正常運作

### 優先級 2：新增自訂域名（可選）

如果需要使用自訂域名（如 `app.ichef.com.tw`）：

1. 在 Cloudflare Pages 專案設定中新增自訂域名
2. 更新 DNS 記錄指向 Pages
3. 更新 `CORS_ORIGIN` 和 `BETTER_AUTH_URL`
4. 重新部署 Server

### 優先級 3：設定 CI/CD（可選）

可以設定 GitHub Actions 自動部署：

```yaml
name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - 'apps/web/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v1
      - run: bun install
      - run: cd apps/web && bun run build
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy dist --project-name=sales-ai-web
```

---

## 📝 變更歷史

- **2026-01-20**:
  - Web 應用部署到 Cloudflare Pages
  - 更新 Server CORS 設定
  - 啟用 Google OAuth 登入
  - 設定三級權限控制

---

**部署完成！** 🎉

現在您可以訪問：https://sales-ai-web.pages.dev/login
