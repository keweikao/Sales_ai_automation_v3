# Sales AI Automation V3 - Q1-Q3 三個月開發排程規劃

**規劃期間**: 2026/01/22 - 2026/04/21
**規劃日期**: 2026-01-21
**版本**: v1.0

---

## 📋 執行摘要

基於上週完成的多產品線支援(iCHEF + Beauty)和 Web 應用部署，接下來三個月將聚焦於:

### 第一個月(1/22-2/21):穩定與優化 🛡️
- 系統監控與告警建立
- 業務反饋收集與改進
- MEDDIC 分析精準度提升
- Web Dashboard 數據視覺化

### 第二個月(2/22-3/21):數據驅動與智能化 🧠
- 話術知識庫建立
- 競品情報系統
- 業務技能追蹤
- 自動化跟進系統

### 第三個月(3/22-4/21):擴展與規模化 🚀
- MQL 自動評分
- Lead Source 追蹤
- 系統效能優化
- 規模化準備

---

## 🎯 第一個月:穩定與優化(Week 1-4)

### Week 1(1/22-1/26):監控體系建立

#### 任務 1.1:生產環境監控系統 🔥
**優先級**: P0(緊急)
**預估工時**: 2 天

**背景**:
目前系統已部署生產環境，但缺乏完整監控，無法及時發現問題。

**實作項目**:

1. **Sentry 錯誤追蹤整合**
   ```bash
   # 安裝依賴
   bun add @sentry/node @sentry/browser @sentry/vite-plugin
   ```

   修改檔案:
   - `apps/server/src/index.ts` - Server 端 Sentry 初始化
   - `apps/web/src/main.tsx` - Web 端 Sentry 初始化
   - `apps/queue-worker/src/index.ts` - Worker 端 Sentry 初始化

   設定項目:
   - 錯誤上下文:userId, email, conversationId, productLine
   - Alert Rules:錯誤率 > 5% 立即通知
   - Performance Monitoring:API 回應時間追蹤

2. **健康檢查 API 實作**

   新增檔案:`packages/api/src/routers/health.ts`
   ```typescript
   export const healthRouter = router({
     // 完整健康檢查
     check: publicProcedure.query(async ({ ctx }) => {
       const checks = {
         database: await checkDatabase(ctx.db),
         r2: await checkR2(ctx.env),
         queue: await checkQueue(ctx.env),
         apiKeys: await checkApiKeys(ctx.env),
       };

       const healthy = Object.values(checks).every(c => c.healthy);

       return {
         status: healthy ? 'healthy' : 'unhealthy',
         timestamp: new Date().toISOString(),
         checks,
       };
     }),

     // 存活探針(最小檢查)
     live: publicProcedure.query(() => ({ status: 'alive' })),

     // 就緒探針(能否接受請求)
     ready: publicProcedure.query(async ({ ctx }) => {
       const dbOk = await checkDatabase(ctx.db);
       return { status: dbOk.healthy ? 'ready' : 'not-ready' };
     }),
   });
   ```

3. **Cloudflare Analytics Dashboard 設定**

   監控指標:
   - API 平均回應時間(目標 < 500ms)
   - Queue Worker 平均處理時間(目標 < 60s)
   - 錯誤率(目標 < 1%)
   - 音檔轉錄成功率(目標 > 95%)

   設定 Alert:
   - 5xx 錯誤率 > 5% → 發送 Email/Slack
   - API P95 延遲 > 1s → 發送警告
   - Queue 處理失敗 > 10% → 立即通知

**驗收標準**:
- ✅ Sentry 正確捕獲錯誤並發送通知
- ✅ `/api/health` 端點回傳完整健康狀態
- ✅ Cloudflare Dashboard 顯示關鍵指標
- ✅ 測試觸發告警並收到通知

**產出**:
- [apps/server/src/middleware/sentry.ts](apps/server/src/middleware/sentry.ts)
- [packages/api/src/routers/health.ts](packages/api/src/routers/health.ts)
- [.doc/20260126_監控系統設定完成報告.md](.doc/20260126_監控系統設定完成報告.md)

---

#### 任務 1.2:Beauty Bot 最後設定 ✅
**優先級**: P0(緊急)
**預估工時**: 5 分鐘

**實作**:
```bash
cd apps/slack-bot-beauty
wrangler secret put API_TOKEN
# 輸入與 iCHEF Bot 相同的 token
```

**測試**:
1. 在 Slack DM @Beauty Sales Bot
2. 上傳測試音檔
3. 填寫表單並提交
4. 驗證完整流程成功

**驗收標準**:
- ✅ Beauty Bot 完整 E2E 流程成功
- ✅ 資料庫正確記錄 product_line='beauty'

---

### Week 2(1/27-2/2):業務反饋驅動改進

#### 任務 2.1:業務使用反饋收集 📊
**優先級**: P1(重要)
**預估工時**: 2 天

**執行方式**:

1. **定量數據收集**(從資料庫和日誌)

   建立分析腳本:`scripts/analyze-usage-metrics.ts`
   ```typescript
   // 統計項目
   - 過去 2 週音檔上傳次數(按產品線、按業務)
   - MEDDIC 分析完成率
   - 平均處理時間
   - 錯誤發生次數和類型
   - 高分商機(>75 分)數量
   - Slack 通知點擊率
   ```

2. **定性訪談**(業務訪談)

   訪談對象:
   - iCHEF 業務:2-3 位重度使用者
   - Beauty 業務:2-3 位重度使用者
   - Manager:1 位

   訪談問題:
   ```
   1. MEDDIC 分析結果是否準確?哪個維度最有幫助?
   2. 有沒有誤判的案例?能否提供對話範例?
   3. Slack 通知格式是否清晰?資訊是否足夠?
   4. 表單欄位是否需要調整?
   5. 使用流程是否順暢?有無痛點?
   6. 希望新增什麼功能?
   ```

3. **建立反饋追蹤系統**

   新增資料表:`feedback`
   ```sql
   CREATE TABLE feedback (
     id UUID PRIMARY KEY,
     user_id TEXT NOT NULL,
     conversation_id UUID REFERENCES conversations(id),
     feedback_type TEXT NOT NULL, -- 'bug', 'feature', 'improvement'
     description TEXT NOT NULL,
     priority TEXT DEFAULT 'medium', -- 'low', 'medium', 'high'
     status TEXT DEFAULT 'open', -- 'open', 'in-progress', 'resolved'
     created_at TIMESTAMP DEFAULT NOW()
   );
   ```

   Slack 整合:分析完成後詢問「此次分析是否有幫助?」
   - 👍 有幫助
   - 👎 不準確(請說明)
   - 💡 建議改進

**產出**:
- 使用數據分析報告(Excel/Google Sheets)
- 訪談紀錄與洞察總結
- [.doc/20260202_業務反饋分析報告.md](.doc/20260202_業務反饋分析報告.md)
- 改進需求優先級清單

---

#### 任務 2.2:MEDDIC Prompts 精煉 🎯
**優先級**: P1(重要)
**預估工時**: 3 天

**背景**:
根據業務反饋，可能發現某些 MEDDIC 維度分析不夠準確，需要優化提示詞。

**實作項目**:

1. **建立 Prompts 評估機制**

   收集評估資料:
   - 找出 20 個實際對話案例(10 個 iCHEF + 10 個 Beauty)
   - 包含高分(>75)、中分(50-75)、低分(<50)案例
   - 業務手動評分每個維度(1-5 分)
   - 對比 AI 評分與人工評分的差異

2. **識別優化重點**

   分析低分案例的共通問題:
   - Metrics Agent:是否正確識別營收數字?
   - Pain Agent:是否漏掉關鍵痛點?
   - Decision Process:是否準確判斷決策階段?
   - Champion:是否誤判支持者態度?

3. **Prompts 優化策略**

   修改檔案:
   - `packages/services/prompts/meddic/ichef/*.md`
   - `packages/services/prompts/meddic/beauty/*.md`

   優化方向:
   ```markdown
   ## Metrics Agent 優化
   - 加強營收計算邏輯提示
   - 新增「如果客戶未明確說明數字，詢問客戶規模並估算」
   - 提供產業基準參考

   ## Pain Agent 優化
   - 增加常見痛點模板庫
   - iCHEF: 人力短缺、訂單管理混亂、對帳困難
   - Beauty: 預約衝突、客戶流失、No-show 問題

   ## Decision Process 優化
   - 加強決策階段判斷提示
   - 明確定義「評估中」vs「決策階段」的差異

   ## Champion 優化
   - 加強支持者辨識邏輯
   - 識別「主動推薦」vs「被動接受」的差異
   ```

4. **A/B 測試機制**

   修改檔案:`packages/services/src/llm/prompt-loader.ts`
   ```typescript
   // 支援 Prompt 版本控制
   export function loadMeddicPrompts(
     productLine: ProductLine,
     version: 'v1.0' | 'v1.1' = 'v1.0'
   ): MeddicPrompts {
     const path = `prompts/compiled/${productLine}/${version}`;
     // ...
   }

   // 10% 流量使用 v1.1 測試
   const version = Math.random() < 0.1 ? 'v1.1' : 'v1.0';
   ```

5. **重新編譯 Prompts**
   ```bash
   bun run build:prompts
   ```

**驗收標準**:
- ✅ 收集 20 個評估案例並完成人工評分
- ✅ 識別出至少 3 個優化方向
- ✅ 完成 Prompts v1.1 版本
- ✅ A/B 測試機制上線
- ✅ 1 週後對比 v1.0 vs v1.1 準確度

**產出**:
- Prompts 評估資料(Google Sheets)
- [.doc/20260202_MEDDIC_Prompts優化報告.md](.doc/20260202_MEDDIC_Prompts優化報告.md)
- `packages/services/prompts/compiled/v1.1/` 目錄

---

### Week 3(2/3-2/9):Web 應用強化

#### 任務 3.1:Dashboard 數據視覺化升級 📈
**優先級**: P1(重要)
**預估工時**: 3 天

**目標**:
讓 Manager 更容易看到團隊表現，業務更容易追蹤自己的商機。

**實作項目**:

1. **安裝圖表庫**
   ```bash
   cd apps/web
   bun add recharts date-fns
   ```

2. **新增 Analytics API**

   新增檔案:`packages/api/src/routers/analytics.ts`
   ```typescript
   export const analyticsRouter = router({
     // 團隊概覽
     teamOverview: protectedProcedure
       .input(z.object({
         startDate: z.string(),
         endDate: z.string(),
         productLine: z.enum(['ichef', 'beauty']).optional(),
       }))
       .query(async ({ input, ctx }) => {
         return {
           totalOpportunities: 0,
           averageMeddicScore: 0,
           closeNowCount: 0,
           byStage: {},
           byRep: [],
           scoreTrend: [],
         };
       }),

     // 商機漏斗
     opportunityFunnel: protectedProcedure
       .input(z.object({ productLine: z.enum(['ichef', 'beauty']).optional() }))
       .query(async ({ input, ctx }) => {
         // 統計各階段商機數量
         return {
           stages: [
             { name: 'Discovery', count: 45, conversionRate: 100 },
             { name: 'Qualification', count: 32, conversionRate: 71 },
             { name: 'Proposal', count: 18, conversionRate: 56 },
             { name: 'Negotiation', count: 12, conversionRate: 67 },
             { name: 'Closed Won', count: 8, conversionRate: 67 },
           ],
         };
       }),

     // 高頻痛點分析
     topPainPoints: protectedProcedure
       .input(z.object({ productLine: z.enum(['ichef', 'beauty']).optional() }))
       .query(async ({ input, ctx }) => {
         // 從 MEDDIC 分析中提取痛點關鍵字
         return {
           painPoints: [
             { keyword: '人力短缺', count: 23, avgScore: 72 },
             { keyword: '訂單管理', count: 18, avgScore: 68 },
             { keyword: '對帳困難', count: 15, avgScore: 71 },
           ],
         };
       }),
   });
   ```

3. **Dashboard 頁面實作**

   新增檔案:`apps/web/src/routes/dashboard/analytics.tsx`
   ```tsx
   import { BarChart, Bar, LineChart, Line, PieChart, Pie } from 'recharts';

   export default function AnalyticsDashboard() {
     const { data: overview } = api.analytics.teamOverview.useQuery({
       startDate: startOfMonth(new Date()).toISOString(),
       endDate: new Date().toISOString(),
     });

     return (
       <div className="space-y-6">
         {/* KPI 卡片 */}
         <div className="grid grid-cols-4 gap-4">
           <KPICard title="本月商機" value={overview.totalOpportunities} />
           <KPICard title="平均 MEDDIC 分數" value={overview.averageMeddicScore} />
           <KPICard title="Close Now 機會" value={overview.closeNowCount} urgent />
           <KPICard title="轉換率" value="67%" />
         </div>

         {/* MEDDIC 分數趨勢圖 */}
         <Card>
           <CardHeader>MEDDIC 分數趨勢</CardHeader>
           <CardContent>
             <LineChart data={overview.scoreTrend}>
               <Line dataKey="avgScore" stroke="#8884d8" />
             </LineChart>
           </CardContent>
         </Card>

         {/* 商機漏斗 */}
         <OpportunityFunnel />

         {/* 業務排行榜 */}
         <RepLeaderboard data={overview.byRep} />

         {/* 高頻痛點詞雲 */}
         <PainPointsWordCloud />
       </div>
     );
   }
   ```

4. **資料聚合優化**

   避免 N+1 查詢問題:
   ```typescript
   // ❌ 不好 - N+1 查詢
   const opportunities = await db.query.opportunities.findMany();
   for (const opp of opportunities) {
     const conversations = await db.query.conversations.findMany({
       where: eq(conversations.opportunityId, opp.id),
     });
   }

   // ✅ 好 - 使用 JOIN
   const data = await db
     .select({
       opportunityId: opportunities.id,
       conversationCount: sql<number>`COUNT(${conversations.id})`,
       avgScore: sql<number>`AVG(${meddicAnalyses.totalScore})`,
     })
     .from(opportunities)
     .leftJoin(conversations, eq(conversations.opportunityId, opportunities.id))
     .leftJoin(meddicAnalyses, eq(meddicAnalyses.conversationId, conversations.id))
     .groupBy(opportunities.id);
   ```

**驗收標準**:
- ✅ 團隊概覽頁顯示 4 個 KPI 卡片
- ✅ MEDDIC 分數趨勢圖正確顯示
- ✅ 商機漏斗視覺化完成
- ✅ 業務排行榜功能正常
- ✅ 所有圖表資料正確
- ✅ 查詢效能 < 1s

**產出**:
- [apps/web/src/routes/dashboard/analytics.tsx](apps/web/src/routes/dashboard/analytics.tsx)
- [packages/api/src/routers/analytics.ts](packages/api/src/routers/analytics.ts)
- [.doc/20260209_Dashboard數據視覺化完成報告.md](.doc/20260209_Dashboard數據視覺化完成報告.md)

---

#### 任務 3.2:對話搜尋與過濾功能 🔍
**優先級**: P1(重要)
**預估工時**: 2 天

**目標**:
快速找到相關對話，提升業務工作效率。

**實作項目**:

1. **全文搜尋索引建立**

   Migration:`packages/db/src/migrations/0007_add_fulltext_search.sql`
   ```sql
   -- 新增全文搜尋索引
   ALTER TABLE conversations
   ADD COLUMN search_vector tsvector;

   -- 更新 search_vector
   UPDATE conversations
   SET search_vector =
     to_tsvector('english', COALESCE(title, '')) ||
     to_tsvector('english', COALESCE(summary, ''));

   -- 建立 GIN 索引
   CREATE INDEX conversations_search_idx
   ON conversations USING GIN(search_vector);

   -- 自動更新 trigger
   CREATE FUNCTION conversations_search_trigger() RETURNS trigger AS $$
   BEGIN
     NEW.search_vector :=
       to_tsvector('english', COALESCE(NEW.title, '')) ||
       to_tsvector('english', COALESCE(NEW.summary, ''));
     RETURN NEW;
   END
   $$ LANGUAGE plpgsql;

   CREATE TRIGGER conversations_search_update
   BEFORE INSERT OR UPDATE ON conversations
   FOR EACH ROW EXECUTE FUNCTION conversations_search_trigger();
   ```

2. **搜尋 API 實作**

   修改檔案:`packages/api/src/routers/conversation.ts`
   ```typescript
   // 新增 search procedure
   search: protectedProcedure
     .input(z.object({
       query: z.string().min(1),
       filters: z.object({
         productLine: z.enum(['ichef', 'beauty']).optional(),
         minScore: z.number().min(0).max(100).optional(),
         maxScore: z.number().min(0).max(100).optional(),
         stage: opportunityStageSchema.optional(),
         startDate: z.string().optional(),
         endDate: z.string().optional(),
         repUserId: z.string().optional(),
       }).optional(),
       limit: z.number().default(20),
       offset: z.number().default(0),
     }))
     .query(async ({ input, ctx }) => {
       const { query, filters, limit, offset } = input;

       // 建立查詢條件
       const conditions = [
         sql`${conversations.searchVector} @@ plainto_tsquery('english', ${query})`,
       ];

       if (filters?.productLine) {
         conditions.push(eq(opportunities.productLine, filters.productLine));
       }

       if (filters?.minScore !== undefined) {
         conditions.push(gte(meddicAnalyses.totalScore, filters.minScore));
       }

       // ... 其他 filters

       const results = await ctx.db
         .select({
           conversation: conversations,
           opportunity: opportunities,
           meddicScore: meddicAnalyses.totalScore,
           rank: sql<number>`ts_rank(${conversations.searchVector}, plainto_tsquery('english', ${query}))`,
         })
         .from(conversations)
         .leftJoin(opportunities, eq(conversations.opportunityId, opportunities.id))
         .leftJoin(meddicAnalyses, eq(meddicAnalyses.conversationId, conversations.id))
         .where(and(...conditions))
         .orderBy(desc(sql`rank`))
         .limit(limit)
         .offset(offset);

       return results;
     }),
   ```

3. **搜尋 UI 實作**

   新增元件:`apps/web/src/components/conversation-search.tsx`
   ```tsx
   export function ConversationSearch() {
     const [query, setQuery] = useState('');
     const [filters, setFilters] = useState<SearchFilters>({});

     const { data, isLoading } = api.conversation.search.useQuery({
       query,
       filters,
     }, {
       enabled: query.length > 0,
     });

     return (
       <div>
         {/* 搜尋輸入框 */}
         <Input
           placeholder="搜尋對話內容、摘要..."
           value={query}
           onChange={(e) => setQuery(e.target.value)}
         />

         {/* 進階過濾器 */}
         <div className="flex gap-2 mt-4">
           <Select
             placeholder="產品線"
             onValueChange={(v) => setFilters({ ...filters, productLine: v })}
           >
             <SelectItem value="ichef">iCHEF</SelectItem>
             <SelectItem value="beauty">Beauty</SelectItem>
           </Select>

           <div className="flex gap-2">
             <Input
               type="number"
               placeholder="最低分數"
               onChange={(e) => setFilters({ ...filters, minScore: Number(e.target.value) })}
             />
             <Input
               type="number"
               placeholder="最高分數"
               onChange={(e) => setFilters({ ...filters, maxScore: Number(e.target.value) })}
             />
           </div>

           <DateRangePicker
             onChange={(range) => setFilters({ ...filters, ...range })}
           />
         </div>

         {/* 搜尋結果 */}
         <div className="mt-6">
           {isLoading ? (
             <LoadingSpinner />
           ) : (
             <ConversationList conversations={data} />
           )}
         </div>
       </div>
     );
   }
   ```

4. **儲存搜尋條件功能**

   新增資料表:`saved_searches`
   ```sql
   CREATE TABLE saved_searches (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     user_id TEXT NOT NULL,
     name TEXT NOT NULL,
     query TEXT,
     filters JSONB NOT NULL,
     created_at TIMESTAMP DEFAULT NOW()
   );
   ```

**驗收標準**:
- ✅ 全文搜尋正確運作
- ✅ 所有過濾器功能正常
- ✅ 搜尋結果按相關度排序
- ✅ 搜尋效能 < 500ms
- ✅ 儲存搜尋條件功能正常

**產出**:
- [packages/db/src/migrations/0007_add_fulltext_search.sql](packages/db/src/migrations/0007_add_fulltext_search.sql)
- [apps/web/src/components/conversation-search.tsx](apps/web/src/components/conversation-search.tsx)
- [.doc/20260209_對話搜尋功能完成報告.md](.doc/20260209_對話搜尋功能完成報告.md)

---

### Week 4(2/10-2/16):Alert 智能化

#### 任務 4.1:Alert 警示系統強化 🚨
**優先級**: P1(重要)
**預估工時**: 3 天

**目標**:
主動發現商機風險與機會，不讓任何機會流失。

**實作項目**:

1. **Alert Rules Engine 架構**

   新增檔案:`packages/services/src/alerts/rules-engine.ts`
   ```typescript
   export interface AlertRule {
     id: string;
     name: string;
     type: 'opportunity' | 'risk';
     condition: (data: AlertContext) => boolean;
     action: (data: AlertContext) => Promise<void>;
     priority: 'low' | 'medium' | 'high' | 'urgent';
   }

   export class AlertRulesEngine {
     private rules: AlertRule[] = [];

     registerRule(rule: AlertRule) {
       this.rules.push(rule);
     }

     async evaluate(conversation: Conversation) {
       for (const rule of this.rules) {
         const context = await this.buildContext(conversation);

         if (rule.condition(context)) {
           await rule.action(context);
           await this.logAlert(rule, context);
         }
       }
     }
   }
   ```

2. **Alert 規則定義**

   新增檔案:`packages/services/src/alerts/rules/index.ts`
   ```typescript
   // Rule 1: Close Now 機會
   export const closeNowAlert: AlertRule = {
     id: 'close-now',
     name: 'Close Now 機會警示',
     type: 'opportunity',
     priority: 'urgent',
     condition: (ctx) => {
       const { meddic } = ctx;
       return (
         meddic.totalScore > 70 &&
         meddic.decisionProcess > 70 &&
         meddic.economicBuyer > 60
       );
     },
     action: async (ctx) => {
       await sendSlackMessage({
         channel: ctx.rep.slackId,
         blocks: [
           {
             type: 'header',
             text: { type: 'plain_text', text: '🔥 Close Now 機會!' },
           },
           {
             type: 'section',
             text: {
               type: 'mrkdwn',
               text: `*${ctx.opportunity.companyName}* 已準備好成交!\n\n` +
                     `MEDDIC 分數: ${ctx.meddic.totalScore}/100\n` +
                     `建議行動: 立即聯繫並提出正式提案`,
             },
           },
           {
             type: 'actions',
             elements: [
               { type: 'button', text: '查看詳情', url: ctx.conversationUrl },
               { type: 'button', text: '準備提案', style: 'primary' },
             ],
           },
         ],
       });

       // 同時通知 Manager
       await sendSlackMessage({
         channel: ctx.manager.slackId,
         text: `${ctx.rep.name} 有一個 Close Now 機會: ${ctx.opportunity.companyName}`,
       });
     },
   };

   // Rule 2: Missing Decision Maker
   export const missingDMAlert: AlertRule = {
     id: 'missing-dm',
     name: 'Missing Decision Maker 警示',
     type: 'risk',
     priority: 'high',
     condition: (ctx) => {
       const daysSinceCreated = differenceInDays(new Date(), ctx.opportunity.createdAt);
       return (
         ctx.meddic.economicBuyer < 40 &&
         daysSinceCreated >= 3
       );
     },
     action: async (ctx) => {
       await sendSlackMessage({
         channel: ctx.rep.slackId,
         blocks: [
           {
             type: 'section',
             text: {
               type: 'mrkdwn',
               text: `⚠️ *${ctx.opportunity.companyName}* 尚未接觸到決策者\n\n` +
                     `建議行動:\n` +
                     `• 詢問現在對話的人誰有最終決策權\n` +
                     `• 要求引薦給關鍵決策者\n` +
                     `• 了解決策流程和時間表`,
             },
           },
         ],
       });
     },
   };

   // Rule 3: 商機停滯
   export const stalledOpportunityAlert: AlertRule = {
     id: 'stalled-opportunity',
     name: '商機停滯警示',
     type: 'risk',
     priority: 'medium',
     condition: (ctx) => {
       const daysSinceLastConversation = differenceInDays(
         new Date(),
         ctx.opportunity.lastConversationAt
       );
       return daysSinceLastConversation >= 7;
     },
     action: async (ctx) => {
       const suggestedTalkTracks = await getTalkTracks({
         scenario: 'follow-up',
         productLine: ctx.opportunity.productLine,
       });

       await sendSlackMessage({
         channel: ctx.rep.slackId,
         blocks: [
           {
             type: 'section',
             text: {
               type: 'mrkdwn',
               text: `📅 *${ctx.opportunity.companyName}* 已 ${daysSinceLastConversation} 天無新進展\n\n` +
                     `建議話術:\n${suggestedTalkTracks.slice(0, 2).map(t => `• ${t.text}`).join('\n')}`,
             },
           },
         ],
       });
     },
   };

   // Rule 4: 高價值商機追蹤
   export const highValueAlert: AlertRule = {
     id: 'high-value',
     name: '高價值商機追蹤',
     type: 'opportunity',
     priority: 'high',
     condition: (ctx) => {
       const threshold = ctx.opportunity.productLine === 'ichef' ? 500000 : 200000;
       return (ctx.meddic.estimatedRevenue || 0) > threshold;
     },
     action: async (ctx) => {
       // 每日發送進度更新給 Manager
       await sendSlackMessage({
         channel: ctx.manager.slackId,
         text: `💰 高價值商機更新: ${ctx.opportunity.companyName} (預估 ${ctx.meddic.estimatedRevenue} 元)\n` +
               `負責業務: ${ctx.rep.name}\n` +
               `當前階段: ${ctx.opportunity.stage}\n` +
               `MEDDIC 分數: ${ctx.meddic.totalScore}/100`,
       });
     },
   };
   ```

3. **Cloudflare Cron Trigger 設定**

   修改檔案:`apps/queue-worker/wrangler.toml`
   ```toml
   # 每 6 小時檢查一次 Alert
   [triggers]
   crons = ["0 */6 * * *"]
   ```

   新增檔案:`apps/queue-worker/src/scheduled.ts`
   ```typescript
   export async function scheduledHandler(
     event: ScheduledEvent,
     env: Env,
     ctx: ExecutionContext
   ) {
     console.log('Running scheduled alert check...');

     // 取得所有進行中的商機
     const activeOpportunities = await db.query.opportunities.findMany({
       where: inArray(opportunities.stage, ['discovery', 'qualification', 'proposal', 'negotiation']),
       with: {
         conversations: {
           with: {
             meddicAnalysis: true,
           },
           orderBy: desc(conversations.createdAt),
           limit: 1,
         },
       },
     });

     // 評估所有規則
     const engine = new AlertRulesEngine();
     engine.registerRule(closeNowAlert);
     engine.registerRule(missingDMAlert);
     engine.registerRule(stalledOpportunityAlert);
     engine.registerRule(highValueAlert);

     for (const opportunity of activeOpportunities) {
       const latestConversation = opportunity.conversations[0];
       if (latestConversation) {
         await engine.evaluate(latestConversation);
       }
     }

     console.log(`Alert check completed. Evaluated ${activeOpportunities.length} opportunities.`);
   }
   ```

4. **Alert 歷史記錄**

   新增資料表:`alerts`
   ```sql
   CREATE TABLE alerts (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     rule_id TEXT NOT NULL,
     opportunity_id UUID REFERENCES opportunities(id),
     conversation_id UUID REFERENCES conversations(id),
     type TEXT NOT NULL, -- 'opportunity', 'risk'
     priority TEXT NOT NULL, -- 'low', 'medium', 'high', 'urgent'
     message TEXT NOT NULL,
     dismissed BOOLEAN DEFAULT FALSE,
     dismissed_at TIMESTAMP,
     dismissed_by TEXT,
     created_at TIMESTAMP DEFAULT NOW()
   );

   CREATE INDEX idx_alerts_opportunity ON alerts(opportunity_id);
   CREATE INDEX idx_alerts_created_at ON alerts(created_at);
   ```

**驗收標準**:
- ✅ 4 個 Alert 規則正確運作
- ✅ Cron job 每 6 小時自動執行
- ✅ Slack 通知格式正確
- ✅ Alert 歷史正確記錄
- ✅ Manager 收到高價值商機通知

**產出**:
- [packages/services/src/alerts/rules-engine.ts](packages/services/src/alerts/rules-engine.ts)
- [apps/queue-worker/src/scheduled.ts](apps/queue-worker/src/scheduled.ts)
- [.doc/20260216_Alert智能化完成報告.md](.doc/20260216_Alert智能化完成報告.md)

---

#### 任務 4.2:第一個月總結與複盤 📝
**優先級**: P0(緊急)
**預估工時**: 1 天

**執行時間**: 2/17-2/21

**複盤項目**:

1. **系統穩定性指標**
   - 可用性(目標 99.5%)
   - 平均 API 回應時間
   - 錯誤率
   - 音檔處理成功率

2. **業務使用數據**
   - 音檔上傳量(對比上月)
   - 分析完成率
   - 業務滿意度(訪談結果)
   - Dashboard 活躍用戶數

3. **功能完成度**
   - 已完成功能清單
   - 未完成項目分析
   - 技術債狀況

4. **下個月規劃調整**
   - 根據業務反饋調整優先級
   - 識別新的痛點
   - 規劃新功能

**產出**:
- [.doc/20260221_第一個月總結報告.md](.doc/20260221_第一個月總結報告.md)
- Q2 第二個月優先級清單

---

## 🧠 第二個月:數據驅動與智能化(Week 5-8)

### Week 5(2/22-2/28):話術知識庫

#### 任務 5.1:Talk Tracks 知識庫系統 💬
**優先級**: P2(中等)
**預估工時**: 4 天

**目標**:
將成功話術系統化，幫助業務複製成功經驗。

**實作項目**:

1. **話術萃取引擎**

   新增檔案:`packages/services/src/talk-tracks/extractor.ts`
   ```typescript
   export async function extractTalkTracks(
     conversation: Conversation,
     meddicScore: number
   ): Promise<ExtractedTalkTrack[]> {
     // 只從高分對話萃取(>75 分)
     if (meddicScore < 75) return [];

     const prompt = `
     分析以下銷售對話，提取成功的話術技巧:

     對話內容:
     ${conversation.transcript}

     MEDDIC 分數: ${meddicScore}/100

     請按以下場景分類提取話術:
     1. 開場破冰 - 建立信任、打開話題的技巧
     2. 痛點挖掘 - 引導客戶說出痛點的提問方式
     3. 價值主張 - 說明產品價值的方式
     4. 異議處理 - 處理客戶疑慮的話術
     5. 促成成交 - 推進成交的技巧

     對於每個話術，請提供:
     - 場景分類
     - 話術文字(保持原始用詞)
     - 為什麼有效的分析
     - 適用情境

     輸出 JSON 格式。
     `;

     const result = await gemini.generateContent(prompt);
     return parseExtractedTalkTracks(result.text);
   }
   ```

2. **話術資料庫 Schema**

   Migration:`packages/db/src/migrations/0008_add_talk_tracks.sql`
   ```sql
   CREATE TABLE talk_tracks (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     product_line TEXT NOT NULL, -- 'ichef', 'beauty'
     scenario TEXT NOT NULL, -- 'opening', 'pain_discovery', 'value_prop', 'objection', 'closing'
     talk_track_text TEXT NOT NULL,
     context TEXT, -- 為什麼有效、適用情境
     effectiveness_score INTEGER DEFAULT 0, -- 使用者評分累計
     usage_count INTEGER DEFAULT 0, -- 被推薦次數
     helpful_count INTEGER DEFAULT 0, -- 被標記有用次數
     source_conversation_id UUID REFERENCES conversations(id),
     created_by TEXT,
     created_at TIMESTAMP DEFAULT NOW()
   );

   CREATE INDEX idx_talk_tracks_product_line ON talk_tracks(product_line);
   CREATE INDEX idx_talk_tracks_scenario ON talk_tracks(scenario);
   CREATE INDEX idx_talk_tracks_effectiveness ON talk_tracks(effectiveness_score DESC);
   ```

3. **話術推薦 API**

   新增檔案:`packages/api/src/routers/talk-tracks.ts`
   ```typescript
   export const talkTracksRouter = router({
     // 取得推薦話術
     recommend: protectedProcedure
       .input(z.object({
         conversationId: z.string().uuid(),
         scenario: z.enum(['opening', 'pain_discovery', 'value_prop', 'objection', 'closing']),
         limit: z.number().default(3),
       }))
       .query(async ({ input, ctx }) => {
         const conversation = await getConversation(input.conversationId);

         // 使用 Embedding 相似度匹配
         const conversationEmbedding = await generateEmbedding(conversation.summary);

         const results = await ctx.db
           .select()
           .from(talkTracks)
           .where(
             and(
               eq(talkTracks.productLine, conversation.opportunity.productLine),
               eq(talkTracks.scenario, input.scenario)
             )
           )
           .orderBy(desc(talkTracks.effectivenessScore))
           .limit(input.limit * 3); // 取多一些候選

         // 計算相似度並排序
         const ranked = results.map(track => ({
           ...track,
           similarity: cosineSimilarity(conversationEmbedding, track.embedding),
         })).sort((a, b) => b.similarity - a.similarity);

         return ranked.slice(0, input.limit);
       }),

     // 標記話術有用/無用
     feedback: protectedProcedure
       .input(z.object({
         talkTrackId: z.string().uuid(),
         helpful: z.boolean(),
       }))
       .mutation(async ({ input, ctx }) => {
         await ctx.db
           .update(talkTracks)
           .set({
             helpfulCount: sql`${talkTracks.helpfulCount} + ${input.helpful ? 1 : 0}`,
             usageCount: sql`${talkTracks.usageCount} + 1`,
             effectivenessScore: sql`${talkTracks.helpfulCount} * 100 / NULLIF(${talkTracks.usageCount}, 0)`,
           })
           .where(eq(talkTracks.id, input.talkTrackId));
       }),
   });
   ```

4. **Slack 整合**

   修改檔案:`apps/queue-worker/src/index.ts`
   ```typescript
   // 分析完成後，自動推薦話術
   const recommendedTalkTracks = await api.talkTracks.recommend({
     conversationId: conversation.id,
     scenario: determineScenario(meddicAnalysis),
   });

   await slack.chat.postMessage({
     channel: channelId,
     blocks: [
       // ... 現有的 MEDDIC 分析結果 ...
       {
         type: 'divider',
       },
       {
         type: 'section',
         text: {
           type: 'mrkdwn',
           text: '*💬 推薦話術*\n根據類似的成功案例，以下話術可能有幫助:',
         },
       },
       ...recommendedTalkTracks.map(track => ({
         type: 'section',
         text: {
           type: 'mrkdwn',
           text: `> ${track.talkTrackText}\n_${track.context}_`,
         },
         accessory: {
           type: 'button',
           text: { type: 'plain_text', text: '👍 有用' },
           value: track.id,
           action_id: `talk_track_helpful_${track.id}`,
         },
       })),
     ],
   });
   ```

5. **批次萃取現有對話**

   新增腳本:`scripts/extract-talk-tracks-from-existing.ts`
   ```typescript
   // 從現有高分對話中萃取話術
   const highScoreConversations = await db.query.conversations.findMany({
     where: and(
       isNotNull(conversations.transcript),
       gte(meddicAnalyses.totalScore, 75)
     ),
     with: {
       meddicAnalysis: true,
       opportunity: true,
     },
     limit: 100,
   });

   for (const conversation of highScoreConversations) {
     const extractedTracks = await extractTalkTracks(
       conversation,
       conversation.meddicAnalysis.totalScore
     );

     for (const track of extractedTracks) {
       await db.insert(talkTracks).values({
         productLine: conversation.opportunity.productLine,
         scenario: track.scenario,
         talkTrackText: track.text,
         context: track.context,
         sourceConversationId: conversation.id,
       });
     }
   }
   ```

**驗收標準**:
- ✅ 話術萃取引擎正確運作
- ✅ 從現有對話中萃取至少 50-100 條話術
- ✅ 話術推薦 API 正確運作
- ✅ Slack 自動推薦話術功能上線
- ✅ 業務回饋機制正常

**產出**:
- [packages/services/src/talk-tracks/extractor.ts](packages/services/src/talk-tracks/extractor.ts)
- [packages/db/src/migrations/0008_add_talk_tracks.sql](packages/db/src/migrations/0008_add_talk_tracks.sql)
- 話術資料庫(50-100 條初始資料)
- [.doc/20260228_話術知識庫完成報告.md](.doc/20260228_話術知識庫完成報告.md)

---

### Week 6(3/1-3/7):競品情報系統

#### 任務 6.1:競品資訊追蹤 🎯
**優先級**: P2(中等)
**預估工時**: 3 天

**實作項目**:

1. **競品資料庫建立**

   Migration:`packages/db/src/migrations/0009_add_competitors.sql`
   ```sql
   CREATE TABLE competitors (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     product_line TEXT NOT NULL,
     name TEXT NOT NULL,
     category TEXT, -- 'direct', 'indirect', 'substitute'
     features JSONB,
     pricing JSONB,
     strengths TEXT[],
     weaknesses TEXT[],
     created_at TIMESTAMP DEFAULT NOW()
   );

   CREATE TABLE competitor_mentions (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     conversation_id UUID REFERENCES conversations(id),
     competitor_id UUID REFERENCES competitors(id),
     sentiment TEXT, -- 'positive', 'neutral', 'negative'
     context TEXT, -- 客戶如何提及
     customer_feedback TEXT, -- 客戶對競品的評價
     created_at TIMESTAMP DEFAULT NOW()
   );
   ```

   Seed 資料:`packages/db/src/seed/competitors.ts`
   ```typescript
   const ichefCompetitors = [
     {
       name: 'EATS365',
       category: 'direct',
       features: { pos: true, online_ordering: true, delivery: true },
       pricing: { monthly: 3000, setup: 10000 },
       strengths: ['雲端系統', '多平台整合', '外送平台串接'],
       weaknesses: ['價格較高', '學習曲線陡峭', '客服回應慢'],
     },
     {
       name: 'Inline',
       category: 'direct',
       strengths: ['價格便宜', '操作簡單'],
       weaknesses: ['功能陽春', '報表不足', '穩定性差'],
     },
     // ...
   ];

   const beautyCompetitors = [
     {
       name: 'LINE 預約',
       category: 'substitute',
       strengths: ['免費', '客戶熟悉', '即時通訊'],
       weaknesses: ['無庫存管理', '無客戶資料', '容易遺漏訊息', '專業度不足'],
     },
     {
       name: '紙本預約',
       category: 'substitute',
       weaknesses: ['容易遺失', '無法線上預約', '衝突難管理', '統計困難'],
     },
     // ...
   ];
   ```

2. **競品偵測 Agent**

   新增檔案:`packages/services/src/competitors/detector.ts`
   ```typescript
   export async function detectCompetitorMentions(
     conversation: Conversation
   ): Promise<CompetitorMention[]> {
     const competitors = await getCompetitorsByProductLine(conversation.opportunity.productLine);
     const competitorNames = competitors.map(c => c.name);

     const prompt = `
     分析以下對話，識別是否提及任何競爭對手:

     競爭對手列表: ${competitorNames.join(', ')}

     對話內容:
     ${conversation.transcript}

     對於每個提及的競品，請提供:
     1. 競品名稱
     2. 客戶情緒(positive/neutral/negative)
     3. 提及情境(如何提到的)
     4. 客戶評價(滿意什麼、不滿什麼)

     輸出 JSON 格式。如果沒有提及競品，回傳空陣列。
     `;

     const result = await gemini.generateContent(prompt);
     const mentions = parseCompetitorMentions(result.text);

     // 儲存到資料庫
     for (const mention of mentions) {
       const competitor = competitors.find(c => c.name === mention.name);
       if (competitor) {
         await db.insert(competitorMentions).values({
           conversationId: conversation.id,
           competitorId: competitor.id,
           sentiment: mention.sentiment,
           context: mention.context,
           customerFeedback: mention.feedback,
         });
       }
     }

     return mentions;
   }
   ```

3. **應對話術生成**

   新增檔案:`packages/services/src/competitors/battle-cards.ts`
   ```typescript
   export async function generateBattleCard(
     competitorName: string,
     productLine: ProductLine
   ): Promise<BattleCard> {
     const competitor = await getCompetitorByName(competitorName, productLine);

     return {
       competitor: competitor.name,
       ourAdvantages: [
         // iCHEF vs EATS365
         `價格更透明: 無隱藏費用,按店計價`,
         `客服更即時: 台灣在地團隊,即時中文支援`,
         `功能更實用: 專注餐飲核心需求,不臃腫`,
       ],
       handlePriceObjection: [
         `了解您的考量。EATS365 看起來便宜,但加上各種模組費用後,實際成本可能更高。`,
         `我們的價格包含所有核心功能,沒有隱藏費用。長期來看反而更划算。`,
       ],
       handleFeatureObjection: [
         `EATS365 功能確實很多,但請問您真正會用到的有哪些?`,
         `我們專注在餐廳老闆最需要的功能,避免複雜度影響效率。`,
       ],
       winningStories: [
         `XX 餐廳從 EATS365 轉到 iCHEF,最大的改善是客服回應速度,從 2 天變成 2 小時。`,
       ],
     };
   }
   ```

4. **競品情報儀表板**

   新增頁面:`apps/web/src/routes/competitors/index.tsx`
   ```tsx
   export default function CompetitorsDashboard() {
     const { data: stats } = api.competitors.getStats.useQuery();

     return (
       <div className="space-y-6">
         {/* 最常提及的競品 */}
         <Card>
           <CardHeader>最常被提及的競品</CardHeader>
           <CardContent>
             <BarChart data={stats.topMentioned}>
               <Bar dataKey="count" />
             </BarChart>
           </CardContent>
         </Card>

         {/* 客戶對競品的情緒分布 */}
         <Card>
           <CardHeader>客戶對競品的態度</CardHeader>
           <CardContent>
             <PieChart data={stats.sentimentDistribution} />
           </CardContent>
         </Card>

         {/* 贏單/輸單競品分析 */}
         <Card>
           <CardHeader>贏單 vs 輸單競品</CardHeader>
           <CardContent>
             <ComparisonTable data={stats.winLossAnalysis} />
           </CardContent>
         </Card>
       </div>
     );
   }
   ```

**驗收標準**:
- ✅ 競品資料庫建立完成(iCHEF 5+ 競品, Beauty 3+ 競品)
- ✅ 競品偵測正確運作
- ✅ 應對話術自動生成
- ✅ 競品儀表板正確顯示

**產出**:
- [packages/db/src/migrations/0009_add_competitors.sql](packages/db/src/migrations/0009_add_competitors.sql)
- [packages/services/src/competitors/detector.ts](packages/services/src/competitors/detector.ts)
- [.doc/20260307_競品情報系統完成報告.md](.doc/20260307_競品情報系統完成報告.md)

---

### Week 7(3/8-3/14):業務技能追蹤

#### 任務 7.1:Rep Skills & Performance Tracking 📊
**優先級**: P2(中等)
**預估工時**: 4 天

**實作項目**:(詳細規劃省略,參考上方規劃架構)

1. 技能評分系統
2. 個人績效儀表板
3. 教練建議引擎
4. Manager 團隊視圖

**產出**:
- [.doc/20260314_業務技能追蹤完成報告.md](.doc/20260314_業務技能追蹤完成報告.md)

---

### Week 8(3/15-3/21):自動化跟進系統

#### 任務 8.1:Follow-up 自動化 🤖
**優先級**: P1(重要)
**預估工時**: 3 天

**實作項目**:(詳細規劃省略,參考上方規劃架構)

1. 智能跟進排程
2. 跟進內容建議
3. 跟進效果追蹤
4. Manager 跟進監控

**產出**:
- [.doc/20260321_自動化跟進系統完成報告.md](.doc/20260321_自動化跟進系統完成報告.md)

---

## 🚀 第三個月:擴展與規模化(Week 9-12)

### Week 9(3/22-3/28):MQL 評分系統

#### 任務 9.1:MQL 自動資格審查 ⚖️
**優先級**: P2(中等)
**預估工時**: 3 天

**實作項目**:(詳細規劃省略)

**產出**:
- [.doc/20260328_MQL評分系統完成報告.md](.doc/20260328_MQL評分系統完成報告.md)

---

### Week 10(3/29-4/4):Lead Source 追蹤

#### 任務 10.1:Lead Source 與 UTM 追蹤 🔗
**優先級**: P2(中等)
**預估工時**: 3 天

**實作項目**:(詳細規劃省略)

**產出**:
- [.doc/20260404_Lead_Source追蹤完成報告.md](.doc/20260404_Lead_Source追蹤完成報告.md)

---

### Week 11(4/5-4/11):Workflow 引擎(可選)

#### 任務 11.1:簡易 Workflow 引擎 ⚙️
**優先級**: P3(低優先級,可選)
**預估工時**: 4 天

**實作項目**:(詳細規劃省略)

---

### Week 12(4/12-4/18):效能優化與壓力測試

#### 任務 12.1:系統效能優化 ⚡
**優先級**: P1(重要)
**預估工時**: 4 天

**實作項目**:

1. **資料庫查詢優化**
   - 分析慢查詢(>1s)
   - 新增必要索引
   - 優化 N+1 查詢
   - 實作查詢結果快取

2. **API 回應時間優化**
   - 目標:P95 < 500ms
   - 實作 HTTP 快取 Headers
   - 使用 Cloudflare Cache API

3. **Queue Worker 並行處理**
   - 支援多檔案並行(最多 3 個)
   - 使用 Cloudflare Queue 批次功能

4. **壓力測試**
   - 100 並發音檔上傳
   - 1000 API 請求/分鐘
   - 識別瓶頸

**產出**:
- [.doc/20260418_系統效能優化完成報告.md](.doc/20260418_系統效能優化完成報告.md)

---

### Week 13(4/19-4/21):Q1 總結

#### 任務 13.1:三個月總結與 Q2 規劃 📊
**優先級**: P0(緊急)
**預估工時**: 1 天

**總結項目**:
1. 功能達成度
2. 系統健康度
3. 業務影響
4. 技術債狀況
5. Q2 規劃方向

**產出**:
- [.doc/20260421_Q1總結報告.md](.doc/20260421_Q1總結報告.md)
- [.doc/20260421_Q2開發計畫.md](.doc/20260421_Q2開發計畫.md)

---

## 📊 優先級總覽

### P0 - 必須完成(7 項)
1. 生產環境監控建立
2. Beauty Bot 最後設定
3. Alert 智能化
4. 自動化跟進系統
5. 系統效能優化
6. 第一個月總結
7. Q1 總結

### P1 - 重要(6 項)
8. 業務反饋收集
9. MEDDIC Prompts 精煉
10. Dashboard 數據視覺化
11. 對話搜尋功能
12. (上列 P0 項目也包含部分 P1)

### P2 - 中等(5 項)
13. Talk Tracks 知識庫
14. 競品情報系統
15. 業務技能追蹤
16. MQL 評分系統
17. Lead Source 追蹤

### P3 - 可選(1 項)
18. Workflow 引擎

---

## 🎯 成功指標(KPIs)

### 系統穩定性
- 可用性:> 99.5%
- API P95 回應時間:< 500ms
- 錯誤率:< 1%
- 音檔處理成功率:> 95%

### 業務效益
- 商機數量:成長 50%+
- MEDDIC 平均分:> 65 分
- 業務滿意度:> 4.5/5
- 高分商機(>75)佔比:> 20%

### 使用活躍度
- 每週音檔上傳量:成長 30%+
- Dashboard 活躍用戶:> 80%
- Alert 回應率:> 70%
- Talk Tracks 使用率:> 50%

---

## 💡 執行建議

### 1. 敏捷迭代
- 每個功能先做 MVP
- 快速上線收集反饋
- 根據數據調整優先級

### 2. 業務價值優先
- 優先解決業務痛點
- 技術債適度償還
- 保持每週都有可見成果

### 3. 數據驅動決策
- 建立完善監控
- 追蹤關鍵指標
- 用數據驗證功能效果

### 4. 保持系統穩定
- 充分測試後再上線
- 保持可回滾能力
- 避免過度優化

### 5. 時間分配
- 30% 技術債處理
- 60% 新功能開發
- 10% 探索創新

---

## 📅 檢查點機制

### 每週 Sprint Review
- 回顧本週完成項目
- Demo 新功能
- 收集團隊反饋

### 月末複盤會議
- 回顧本月完成項目
- 分析未完成原因
- 調整下月優先級
- 更新技術債清單

### 季度規劃會議
- 總結季度成果
- 業務滿意度調查
- 制定下季度 OKR
- 技術架構檢視

---

## 🔄 靈活調整機制

本規劃保持靈活性，以下情況可調整優先級:

1. **業務緊急需求**:優先處理業務急迫問題
2. **系統重大 Bug**:立即修復影響使用的錯誤
3. **效益驗證失敗**:根據數據快速調整方向
4. **資源變化**:團隊人力變動時調整排程

---

**規劃原則**:

✅ **第一個月**:穩定基礎,確保系統可靠,收集業務反饋
✅ **第二個月**:數據驅動,建立智能功能,提升分析價值
✅ **第三個月**:規模化準備,自動化流程,優化效能

每個月都有明確主題和可交付成果,同時保持靈活性,可根據實際情況調整優先級。
